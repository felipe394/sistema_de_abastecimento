# PRODUÇÃO COM HTTPS: deixe vazio — o Apache faz proxy de /api/* para o Node.js internamente
# Isso evita Mixed Content (HTTPS página → HTTP api)
VITE_API_URL=

# DESENVOLVIMENTO LOCAL (sem HTTPS): use a porta direta do backend
# VITE_API_URL=http://localhost:3000
